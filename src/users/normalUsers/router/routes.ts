import { Router } from "express";

import { controllerHandler } from "../../../core";

export const userRouter = Router();

// userRouter.use("/admins", adminRouter);
// userRouter.use("/artists", artistRouter);


// userRouter
//     .get("/contributions", controllerHandler.handle)
//     .post("/contributions", controllerHandler.handle);